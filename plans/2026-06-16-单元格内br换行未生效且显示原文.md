---
type: bug
status: draft
priority: P2
created: 2026-06-16
related_specs:
  - specs/模块-parser/      # Markdown 解析（HtmlInline/<br> 处理）
  - specs/模块-preview/      # 表格单元格渲染换行
related_files:
  - src/parser/   # MarkdownParser
  - src/preview/PreviewLayout.cpp   # 单元格 inlineRuns / 换行
  - src/preview/PreviewPainter.cpp  # 单元格绘制
---

# 表格单元格内 `<br>` 未换行且原文显示

## 背景

用户报告（2026-06-16）：

> 单元格中的内容有 `<br>`，是不是应该换行，当前实际没有换行，而且渲染后 `<br>` 也显示了。

GitHub Flavored Markdown 中表格单元格内 `<br>`（或 `<br/>`）应渲染为**换行**。当前：
1. 没有换行（单元格内容仍在一行）；
2. `<br>` 字面量被当文本显示出来。

## 根因方向（待验证）

- 解析层：`<br>` 被当作 `HtmlInline` 节点，`collectInlineRuns` 的 `HtmlInline` 分支
  （PreviewLayout.cpp:743）按**原始文本**渲染（`run.text = child->literal` = "<br>"）→
  原文显示。需识别 `<br>`/`<br/>`/`<br />` 转为换行 run（text = "\n"，类似 LineBreak）。
- 单元格换行：即使产生换行 run，表格 cell 的 layout/paint 是否支持 cell 内多行？
  需确认 cell 高度计算与绘制对 "\n" run 的处理（paintInlineRuns 对 run.text=="\n" 换行，
  但 cell 高度估算 estimateParagraphHeight 是否计入）。

## 目标（验收条件）

- [ ] 单元格内 `<br>` / `<br/>` / `<br />` 渲染为换行，不显示原文
- [ ] 单元格高度正确容纳多行（不与下方行重叠——参考 INV-TABLE-CELL-NO-OVERFLOW）
- [ ] 普通段落内 `<br>` 也换行（一并处理）
- [ ] 复制语义：换行处复制出 `\n`（与渲染一致）
- [ ] 加测试覆盖 `<br>` → 换行

## 进展

- 2026-06-16 创建（用户报告，仅记录）
