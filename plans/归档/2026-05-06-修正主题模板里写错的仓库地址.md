---
date: 2026-05-06
status: completed
completed: 2026-05-06
related_specs:
  - specs/模块-app/12-主题插件系统.md   # 主题文档归属
---

# 修正主题模板里写错的仓库地址

## 背景
用户澄清：**项目仓库地址为 `https://github.com/ssk-wh/simple_markdown`**（与 git remote 一致），但主题文档里**错写成了 `https://github.com/pcfan/simple_markdown`**，要修正。

**调查结果**（已核实）：
- 全项目共有 7 处仓库 URL 引用：
  - ✅ `README.md` line 88：`ssk-wh/simple_markdown`（正确）
  - ✅ `docs/构建说明.md` line 30：`ssk-wh/simple_markdown`（正确）
  - ✅ `debian/control` line 10：`ssk-wh/simple_markdown`（正确）
  - ✅ `debian/copyright` line 3：`ssk-wh/simple_markdown`（正确）
  - ✅ `src/app/MainWindow.cpp` line 623-624：`ssk-wh/simple_markdown`（正确）
  - ❌ `resources/theme_docs/template.toml` line 95：`pcfan/simple_markdown`（**错**，要改）
  - ❌ `resources/theme_docs/HOW_TO.md` line 48 和 line 63：`pcfan/simple_markdown`（**错**，要改）

仅记录待办，本次不修复。

## 动作（修复时）
- [ ] `resources/theme_docs/template.toml` line 95：把 `pcfan/simple_markdown` 改为 `ssk-wh/simple_markdown`
- [ ] `resources/theme_docs/HOW_TO.md` line 48：同上
- [ ] `resources/theme_docs/HOW_TO.md` line 63：同上（issues 链接）
- [ ] 验证：`grep -rn "pcfan/simple_markdown" --exclude-dir=plans` 应返回空（plans 里如有早期讨论可保留为历史）
- [ ] 验证：构建后用户在主题模板里看到的克隆地址 / issues 链接指向真实仓库
- [ ] 不进 CHANGELOG（用户感知层面是"链接错→改对"，属于内部资源勘误，没有可见行为变化；CHANGELOG 写作纪律明确禁止此类条目，归 commit message 即可）

## 验收
- 主题文档（template.toml + HOW_TO.md）打开后看到的所有仓库链接均为 `https://github.com/ssk-wh/simple_markdown` 系列
- 全项目（除 plans/ 下讨论文件）不再出现 `pcfan/simple_markdown` 字样

## 备注
- 这是纯文本勘误，不涉及 Spec 变更、不涉及 i18n（URL 不参与翻译）
- 其他位置（README、debian 元数据、关于对话框）原本就正确，**不要动**
- 我（AI 助手）此前一度误判为"应统一到 pcfan"，是因为把用户原话『**项目的源仓库地址为** pcfan…』理解成了"权威源"——实际用户是说"我看到模板里写的是 pcfan，但项目仓库其实是 ssk-wh"。本次记录后已澄清
