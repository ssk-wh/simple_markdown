---
type: feat
status: completed
priority: P2
created: 2026-06-15
related_specs:
  - specs/模块-app/      # 自动保存设置（需核对/补 Spec）
related_plans:
  - plans/归档/2026-04-14-自动保存已完成.md
related_files:
  - src/app/   # MainWindow 自动保存设置/定时器
---

# 自动保存延时选项调整 + 默认关闭

## 背景

用户需求（2026-06-15）：

> 自动保存延时改为 30s / 5min / 10min，默认不开启自动保存，保存延时默认是 5min。

## 目标（验收条件）

- [ ] 自动保存延时可选项改为三档：**30 秒 / 5 分钟 / 10 分钟**（移除/替换现有档位）
- [ ] 默认**不开启**自动保存（首次启动 / 无历史设置时，自动保存为关闭状态）
- [ ] 自动保存延时的**默认值为 5 分钟**（即用户开启自动保存但未改延时时，用 5min）
- [ ] 设置持久化：开关状态 + 选定延时跨会话保留
- [ ] 相关 UI 文案 tr() 包裹，同步 .ts / lrelease（INV-I18N-*）
- [ ] 核对/补 specs/模块-app 下自动保存相关 Spec（默认状态、档位、持久化）

## 待核对

- [ ] 现有自动保存 UI 在哪（设置菜单/对话框）、现有档位与默认值
- [ ] 现有默认是否已开启（需改为默认关闭）
- [ ] 定时器实现与延时单位

## 进展

- 2026-06-15 创建（用户提需求，仅记录，待实施）

## 进展（补充）
- 2026-06-16 实现：延时档位 kDelays 改为 {30000,300000,600000}（30s/5min/10min）；
  默认 enabled=false、delayMs=300000（头文件成员 + loadSettings 双处）；兜底接受新三档、
  旧小延时归一到 5min。tr() 字符串更新 + zh_CN/en_US .ts 同步（30秒/5分钟/10分钟），
  cmake 构建自动重生成 .qm。Spec 14 档位/默认值更新。CHANGELOG 1.1.10 Changed 段。
  编译 + 16 测试全过。菜单标签确定性由 .ts/.qm 保证，默认关闭需 fresh settings 手动确认。
- 2026-06-16 ✅ 已完成
