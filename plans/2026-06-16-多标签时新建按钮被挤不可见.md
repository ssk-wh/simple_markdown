---
type: bug
status: draft
priority: P2
created: 2026-06-16
related_specs:
  - specs/模块-app/    # 多 Tab / TabBar 布局
related_files:
  - src/app/TabBarWithAdd.cpp   # Tab 栏 + 新建按钮
  - src/app/MainWindow.cpp
---

# 多标签时「新建」按钮被挤出不可见

## 背景

用户报告（2026-06-16）：

> 打开的文档较多之后，tab 上面的「新建」按钮怎么都看不到，非要关闭一些文档才行。

Tab 数量多到撑满 Tab 栏宽度后，跟在 Tab 末尾的「+」新建按钮被挤出可视区域，
用户无法新建文档，只能先关闭部分 Tab。

## 根因方向（待验证）

- TabBarWithAdd 的「+」按钮定位在最后一个 Tab 右侧，Tab 总宽超出栏宽时按钮跟着溢出。
- 需确认 Tab 栏是否启用了滚动/省略策略；「+」按钮应固定可见（如钉在 Tab 栏右端，或
  Tab 区可滚动而「+」常驻）。

## 目标（验收条件）

- [ ] 无论打开多少 Tab，「新建」按钮始终可见可点
- [ ] Tab 过多时 Tab 区可滚动 / 省略，但不挤掉新建按钮

## 进展

- 2026-06-16 创建（用户报告，仅记录）
