# 列表渲染测试

## 无序列表（扁平）

- 无序列表项 1
- 无序列表项 2
- 无序列表项 3

## 有序列表（扁平）

1. 有序列表项 1
2. 有序列表项 2
3. 有序列表项 3

## 嵌套无序列表

- 外层项 A
  - 内层项 A1
  - 内层项 A2
- 外层项 B
  - 内层项 B1
  - 内层项 B2

## 嵌套有序列表（触发双倍偏移的场景）

1. **PrintService 优化**（-5-10% 缩略图时间）
   - IPC 通信效率提升
   - 消息队列处理优化
   - 渲染管道加速
2. **缓存策略**（-30% 内存占用）
   - 限制缓存大小（LRU 淘汰）
   - 懒加载 vs 预加载平衡
   - 及时释放不用的数据
3. **异步化**（-0% CPU 峰值）
   - 后台线程渲染
   - 异步 IPC 通信
   - 非阻塞清理

## 三层嵌套

- 第一层
  - 第二层
    - 第三层 A
    - 第三层 B
  - 第二层（续）
- 第一层（续）

## 列表后的段落

这个段落应该在列表下方，不与列表内容重叠。

---

分割线后的段落，确认列表不会溢出到这里。
