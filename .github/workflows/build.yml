name: Build & Package

on:
  push:
    branches: [master, main]
    tags: ['v*']
  pull_request:
    branches: [master, main]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '5.15.2'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2019_64'
          cache: true

      - name: Install NSIS
        run: choco install nsis -y

      - name: Configure
        run: cmake -S . -B build -A x64

      - name: Build Release
        run: cmake --build build --config Release

      - name: Run Tests
        run: ctest --test-dir build -C Release --output-on-failure

      - name: Extract version
        shell: pwsh
        run: |
          if ("$env:GITHUB_REF" -match "^refs/tags/v(.+)$") {
            $version = $Matches[1]
          } else {
            $content = Get-Content CHANGELOG.md -Raw
            if ($content -match '\[(\d+\.\d+\.\d+)\]') {
              $version = $Matches[1]
            } else {
              $version = "0.1.0"
            }
          }
          echo "VERSION=$version" >> $env:GITHUB_ENV
          echo "Building version: $version"

      - name: Collect dist files
        shell: pwsh
        run: |
          $dist = "installer/dist"
          if (Test-Path $dist) { Remove-Item -Recurse -Force $dist }
          New-Item -ItemType Directory -Force -Path "$dist/platforms","$dist/imageformats","$dist/styles" | Out-Null

          # Qt5_DIR points to lib/cmake/Qt5, resolve Qt root
          $qtRoot = (Resolve-Path "$env:Qt5_DIR/../../..").Path
          if (-not (Test-Path "$qtRoot/bin/Qt5Core.dll")) {
            $qtRoot = (Resolve-Path "$env:Qt5_DIR/../..").Path
          }
          if (-not (Test-Path "$qtRoot/bin/Qt5Core.dll")) {
            $search = $env:Qt5_DIR
            for ($i = 0; $i -lt 5; $i++) {
              $search = Split-Path $search -Parent
              if (Test-Path "$search/bin/Qt5Core.dll") { $qtRoot = $search; break }
            }
          }
          if (-not (Test-Path "$qtRoot/bin/Qt5Core.dll")) {
            Write-Host "::error::Cannot find Qt bin directory. Qt5_DIR=$env:Qt5_DIR"
            exit 1
          }
          $qtBin = "$qtRoot/bin"
          $qtPlugins = "$qtRoot/plugins"
          Write-Host "Qt root: $qtRoot"
          Write-Host "Qt bin: $qtBin"
          Write-Host "Qt plugins: $qtPlugins"

          # Copy exe
          Copy-Item "build/src/app/Release/SimpleMarkdown.exe" "$dist/"

          # Copy Qt DLLs
          foreach ($dll in @("Qt5Core","Qt5Gui","Qt5Widgets","Qt5Network","Qt5Svg","Qt5PrintSupport")) {
            $path = "$qtBin/$dll.dll"
            if (Test-Path $path) { Copy-Item $path "$dist/" }
          }

          # Copy ANGLE/OpenGL
          foreach ($dll in @("libEGL","libGLESv2","D3Dcompiler_47","opengl32sw")) {
            $path = "$qtBin/$dll.dll"
            if (Test-Path $path) { Copy-Item $path "$dist/" }
          }

          # Copy MSVC runtime from Qt bin (if present)
          foreach ($dll in @("vcruntime140.dll","vcruntime140_1.dll","msvcp140.dll","concrt140.dll")) {
            $path = "$qtBin/$dll"
            if (Test-Path $path) { Copy-Item $path "$dist/" }
          }

          # Copy Qt plugins
          Copy-Item "$qtPlugins/platforms/qwindows.dll" "$dist/platforms/"
          foreach ($fmt in @("qjpeg","qgif","qico","qsvg","qwebp","qtiff")) {
            $path = "$qtPlugins/imageformats/$fmt.dll"
            if (Test-Path $path) { Copy-Item $path "$dist/imageformats/" }
          }
          $vistaStyle = "$qtPlugins/styles/qwindowsvistastyle.dll"
          if (Test-Path $vistaStyle) { Copy-Item $vistaStyle "$dist/styles/" }

          $count = (Get-ChildItem -Recurse "$dist" -File).Count
          Write-Host "Collected $count files"

      - name: Build NSIS installer
        shell: pwsh
        run: |
          $nsi = "installer/SimpleMarkdown.nsi"
          # 必须保留 UTF-8 BOM：NSIS `Unicode True` 靠 BOM 识别 UTF-8 源文件；
          # pwsh 的 Set-Content 默认 utf8NoBOM 会剥离 BOM，导致 makensis 按系统代码页
          # 解析中文（如完成页"立即启动"）→ 安装包中文乱码。读写都强制 utf8BOM。
          (Get-Content $nsi -Encoding utf8BOM) -replace '!define APP_VERSION ".*"', "!define APP_VERSION `"$env:VERSION`"" | Set-Content $nsi -Encoding utf8BOM
          $makensis = "${env:ProgramFiles(x86)}\NSIS\makensis.exe"
          if (-not (Test-Path $makensis)) { $makensis = "$env:ProgramFiles\NSIS\makensis.exe" }
          cd installer
          & $makensis SimpleMarkdown.nsi

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: SimpleMarkdown-${{ env.VERSION }}-Setup
          path: installer/SimpleMarkdown-*-Setup.exe

      - name: Create portable zip
        shell: pwsh
        run: |
          Compress-Archive -Path "installer/dist/*" -DestinationPath "installer/SimpleMarkdown-$env:VERSION-Portable.zip"

      - name: Upload portable
        uses: actions/upload-artifact@v4
        with:
          name: SimpleMarkdown-${{ env.VERSION }}-Windows-Portable
          path: installer/SimpleMarkdown-*-Portable.zip

  build-linux:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Extract version
        id: version
        run: |
          if [[ "$GITHUB_REF" =~ ^refs/tags/v(.+)$ ]]; then
            VERSION="${BASH_REMATCH[1]}"
          else
            VERSION=$(grep -oP '^\[\K\d+\.\d+\.\d+(?=\])' CHANGELOG.md | head -1)
            if [ -z "$VERSION" ]; then
              VERSION="0.1.0"
            fi
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building version: $VERSION"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            debhelper cmake qtbase5-dev libqt5svg5-dev \
            qttools5-dev qttools5-dev-tools \
            fakeroot dpkg-dev librsvg2-bin

      - name: Generate PNG icons from SVG
        run: |
          cd resources
          for size in 16 24 32 48 128 256 512; do
            rsvg-convert -w $size -h $size app-icon.svg -o simplemarkdown_${size}.png
          done

      - name: Build DEB package
        run: dpkg-buildpackage -us -uc -b

      - name: CMake build for AppImage
        run: |
          cmake -S . -B build-appimage -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/usr
          cmake --build build-appimage -j$(nproc)
          DESTDIR=$PWD/AppDir cmake --install build-appimage

      - name: Build AppImage
        run: |
          # Download linuxdeploy and Qt plugin
          wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget -q https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          chmod +x linuxdeploy-x86_64.AppImage linuxdeploy-plugin-qt-x86_64.AppImage

          export QMAKE=$(which qmake)
          export VERSION=${{ steps.version.outputs.version }}

          ./linuxdeploy-x86_64.AppImage \
            --appdir AppDir \
            --plugin qt \
            --desktop-file AppDir/usr/share/applications/simplemarkdown.desktop \
            --icon-file resources/simplemarkdown_256.png \
            --output appimage

      - name: Collect artifacts
        run: |
          mkdir -p dist
          cp ../simplemarkdown_*.deb dist/
          cp SimpleMarkdown-*.AppImage dist/ 2>/dev/null || true

      - name: Upload DEB artifact
        uses: actions/upload-artifact@v4
        with:
          name: simplemarkdown_${{ steps.version.outputs.version }}_amd64
          path: dist/*.deb

      - name: Upload AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: SimpleMarkdown-${{ steps.version.outputs.version }}-x86_64.AppImage
          path: dist/*.AppImage

  release:
    needs: [build-windows, build-linux]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/**/*-Setup.exe
            artifacts/**/*-Portable.zip
            artifacts/**/*.deb
            artifacts/**/*.AppImage
          generate_release_notes: true
